# راهنمای راه‌اندازی PWA

پروژه شما اکنون به عنوان Progressive Web App (PWA) پیکربندی شده است و می‌تواند روی گوشی‌های موبایل نصب شود.

## ✅ موارد انجام شده

1. ✅ نصب و پیکربندی `vite-plugin-pwa`
2. ✅ ایجاد `manifest.json` با اطلاعات اپلیکیشن
3. ✅ پیکربندی Service Worker برای عملکرد آفلاین
4. ✅ افزودن Meta Tags به `index.html`
5. ✅ پیکربندی Cache برای فونت‌های Google

## 📱 مراحل نهایی

### 1. تولید آیکون‌ها

برای اینکه اپلیکیشن بتواند روی موبایل نصب شود، نیاز به آیکون‌های PNG دارید:

**روش 1: استفاده از فایل HTML (پیشنهادی)**
1. فایل `public/generate-icons.html` را در مرورگر باز کنید
2. روی دکمه‌های "تولید آیکون 192x192" و "تولید آیکون 512x512" کلیک کنید
3. فایل‌های دانلود شده را در پوشه `public` قرار دهید

**روش 2: استفاده از ابزار آنلاین**
1. به [RealFaviconGenerator](https://realfavicongenerator.net) بروید
2. یک آیکون طراحی کنید یا آپلود کنید
3. آیکون‌های 192x192 و 512x512 را دانلود کنید
4. آنها را در پوشه `public` با نام‌های زیر قرار دهید:
   - `icon-192x192.png`
   - `icon-512x512.png`

### 2. Build و Deploy

```bash
npm run build
```

فایل‌های تولید شده در پوشه `dist` قرار می‌گیرند.

**⚠️ مهم:** PWA نیاز به HTTPS دارد. برای تست محلی می‌توانید از `npm run preview` استفاده کنید، اما برای استفاده واقعی باید روی یک سرور HTTPS deploy کنید.

### 3. نصب روی موبایل

1. پروژه را روی یک سرور HTTPS deploy کنید
2. در مرورگر موبایل (Chrome, Safari, Firefox) به آدرس اپلیکیشن بروید
3. منوی مرورگر را باز کنید و گزینه "Add to Home Screen" یا "نصب" را انتخاب کنید
4. اپلیکیشن مانند یک اپلیکیشن بومی روی موبایل شما نصب می‌شود

## 🎯 ویژگی‌های PWA

- ✅ نصب روی صفحه اصلی موبایل
- ✅ عملکرد آفلاین (با Service Worker)
- ✅ ظاهر اپلیکیشن بومی
- ✅ Cache کردن فونت‌ها و منابع استاتیک
- ✅ پشتیبانی از RTL برای فارسی

## 🔧 تنظیمات

اگر می‌خواهید تنظیمات PWA را تغییر دهید، فایل `vite.config.ts` را ویرایش کنید. بخش `VitePWA` شامل تمام تنظیمات است.

## 📝 یادداشت

- Service Worker به صورت خودکار در production build تولید می‌شود
- در حالت development، Service Worker در حالت module فعال است
- برای تست PWA، از `npm run preview` استفاده کنید

